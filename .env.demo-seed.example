# Client / local demo only — copy to `.env.demo-seed` (gitignored) and fill in.
# Used by: yarn demo:bootstrap, yarn demo:sync-passwords, reset-local-admin-password.mjs
#
#   cp .env.demo-seed.example .env.demo-seed
#
# Never commit `.env.demo-seed` or `.env.demo-seed.local`.

DEMO_UNIFIED_PASSWORD=
